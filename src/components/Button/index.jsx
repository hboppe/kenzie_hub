import StyledButton from "./style";


const Button = ({buttonTitle}) => {
    return (
     
        <StyledButton>
            {buttonTitle}

        </StyledButton>
       
    )
}

export default Button;