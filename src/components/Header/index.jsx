import { useParams } from 'react-router-dom';
import Logo from '../../assets/Logo.svg'

const Header = () => {

    

    return (
        <img src={Logo} alt="" />
        
    )
}

export default Header;